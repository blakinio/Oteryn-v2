[package]
name = "oteryn-diagnostics"
version.workspace = true
description = "Structured bounded and secret-safe diagnostic contracts for Oteryn"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[package.metadata.oteryn]
category = "diagnostics"
release-role = "production"
source-disposition = "MIGRATE_AS_IS_WITH_PROVISIONAL_GENERATION_RENAME"

[dependencies]
oteryn-foundation = { version = "=0.1.0", path = "../foundation" }

[lints]
workspace = true
