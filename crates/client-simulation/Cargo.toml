[package]
name = "oteryn-client-simulation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[package.metadata.oteryn]
category = "client-simulation"
release-role = "synthetic-only"

[dependencies]
oteryn-client-domain = { version = "=0.1.0", path = "../client-domain" }

[dev-dependencies]
oteryn-foundation = { version = "=0.1.0", path = "../foundation" }

[lints]
workspace = true
