[package]
name = "oteryn-renderer"
version.workspace = true
description = "Deterministic DX12 renderer surface ownership for Oteryn"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[package.metadata.oteryn]
category = "renderer"
release-role = "production"
source-disposition = "MIGRATE_AS_IS_AND_SPLIT_GENERIC_RESOURCE_LIFECYCLE"

[dependencies]
oteryn-foundation = { version = "=0.1.0", path = "../foundation" }

[target.'cfg(windows)'.dependencies]
pollster.workspace = true
wgpu.workspace = true

[lints]
workspace = true
